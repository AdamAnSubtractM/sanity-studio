{
  "name": "portfolio-sanity-studio",
  "private": true,
  "version": "1.0.0",
  "main": "package.json",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity start",
    "build": "sanity build",
    "typecheck": "tsc --noEmit",
    "typegen:extract": "sanity schema extract --path=./schema.json",
    "typegen": "pnpm run typegen:extract && sanity typegen generate",
    "publish:types": "pnpm dlx jsr publish",
    "publish:types:dry": "pnpm dlx jsr publish --dry-run --allow-dirty",
    "dataset:export": "sanity dataset export production ./dataset-backups",
    "deploy": "sanity deploy",
    "deploy:graphql": "sanity graphql deploy",
    "lint": "eslint .",
    "format": "prettier --write . && pnpm run lint"
  },
  "keywords": [
    "sanity"
  ],
  "dependencies": {
    "@sanity/vision": "^5.23.0",
    "groq": "^5.23.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "sanity": "^5.23.0",
    "styled-components": "^6.5.3"
  },
  "devDependencies": {
    "@sanity/eslint-config-studio": "^6.0.0",
    "@types/react": "^19.2.18",
    "eslint": "^9.39.4",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
